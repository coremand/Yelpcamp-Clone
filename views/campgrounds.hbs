<!DOCTYPE html>
<html>
    <head>
        <title>
            YelpCamp Webpage
        </title>
        <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
        <body>
            {{>header}}
            <div class="container">
                <header class="jumbotron">
                    <div class="container">
                        <h1>Welcome To YelpCamp</h1>
                        <p>Where Nature Meets Cravings</p>
                        <p>
                            <a class="btn btn-primary btn-large" href="/campgrounds/new">Add New Campgrounds</a>
                        </p>
                    </div>
                </header>
                    <div class="row text-center">
                        {{#each campgrounds}}
                        <div class="col-lg-3 col-md-3 col-sm-6">
                            <div class="card">
                                <img src="{{campgrounds}} {{image}}" class="card-img-top" alt="Photo">
                                <div class="card-body">
                                    <h5 class="card-title">{{campgrounds}} {{name}}</h5>
                                    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
                                    <a href="/campgrounds{{../campground}}/{{_id}}" class="btn btn-primary">Go somewhere</a>
                                </div>
                            </div>
                        </div>
                        {{/each}}
                    </div>
            {{>footer}}
            </div>
        </body>
    </head>
</html>